# BUILD_LOG · Contour (Aesthetic Clinic OS)

One line per completed phase. Newest last. Resume any session from here.

- 2026-07-02 · PLAN: read full knowledge base (11 specs). Mode NEW. Theme locked by KB: Glassmorphic Mint. Stack decision: Next.js 16 + TS + Tailwind v4 (Vercel-native) instead of Vite+Express; client store per KB demo guidance.
- 2026-07-02 · SCAFFOLD: create-next-app (Next 16.2.10, React 19.2, Tailwind v4). Design tokens from 08_design-system.md into globals.css (@theme + .glass recipes + mint slider + motion). Inter via next/font.
- 2026-07-02 · ASSETS: MediaPipe face_landmarker.task (3.7MB) committed to public/models; WASM copied from node_modules via scripts/setup-assets.mjs on postinstall (gitignored, Vercel-safe).
- 2026-07-02 · DATA: types.ts (full KB data model), templates.ts (rhinoplasty: 7 sliders + prompt + 10-item plan; 7 more procedures declared), prompt.ts (phrase bands per KB §4), store.ts (Zustand persist + full actions), seed.ts (Meridian Aesthetics Lahore, 3 staff roles, 3 patients with stories), db.ts (IndexedDB images + landmark cache).
- 2026-07-02 · FACE ENGINE: landmarker.ts (self-hosted, GPU->CPU fallback), geometry.ts (runtime clique triangulation from tessellation, semantic region weights, shared morph engine, canvas->AI param handoff), warp2d.ts (per-triangle affine warp + disclaimer burn-in).
- 2026-07-02 · AI SERVICE: /api/generate (maxDuration 60, validation, clean error codes) + providers.ts (Gemini generateContent inline-image; FLUX.1 Kontext submit/poll fallback; env-switchable). generateClient.ts with params-hash caching.
- 2026-07-02 · MODULES: login (role cards), clinic layout (auth guard + 10-min idle lock), dashboard (stats + today's queue), patients list (search by name/phone/city), registration (search-first dup detection, consent gating, camera modal), patient profile (5 tabs incl. visit timeline), consultation workspace (Brief -> Canvas -> Visualize -> Plan -> Report stepper, front-desk lock), 3D canvas (orbit/draw/sculpt/highlight/scale + morph handles), visualize (7 sliders, live warp preview, AI generate, before/after reveal, save w/ audit), plan (template, checklist CRUD, status flow), report (3 A4 glass sheets, print-exact CSS), settings (staff, templates, branding, demo reset).
- 2026-07-02 · SEED PHOTOS: 3 photoreal patient portraits generated (Higgsfield soul_2, ~0.4 credits total), downscaled to 1024px JPEG (~160KB each), manifest-driven seeding (app degrades gracefully without them).
- 2026-07-02 · VERIFY (browser, all roles): login/quick-roles OK; dashboard stats+queue OK; brief pre-fill OK; MediaPipe pipeline OK (graph runs, face detected); VISUALIZE PROVEN END-TO-END (sliders -> banded prompt -> 2D warp after-image -> reveal slider -> save -> Visualization record with prompt_used -> report shows before/after + param chips); plan template 10 items with dates OK; report 3 sheets OK; front desk: settings hidden, consultation locked, registration full flow (dup warning, consent-gated capture, consent rows with captured_by) OK; admin settings all sections OK; idle auto-lock fired and returned to login OK. FIXED during verify: zustand v5 getSnapshot loop (useShallow on array selectors), firstName honorific, em-dash sweep in UI copy.
- 2026-07-02 · KNOWN ENV LIMIT: preview browser tab is hidden -> requestAnimationFrame never fires -> R3F canvas cannot rasterize there (and screenshot tool times out). The full morph/landmark math is proven via the warp path; 3D scene needs a visible browser tab. Screenshots for the handoff pack pending first run in a visible browser / live deploy.
- 2026-07-03 · SHIP PREP: README, CREDENTIALS.md, DEMO_SCRIPT.md, SYSTEM.md, vercel.json, .env.example, .gitignore (wasm generated), production build green, git commit. Deploy: user pushes to GitHub -> Vercel (steps in README).

- 2026-07-03 · RENAME + PROVIDERS: product renamed Star Fighter -> **Contour** (owner picked from 4 options; Star Fighter was the KB codename). Swept name across UI (Logo Con/tour, title), store persist key (contour-store), demo password (contour), package name, docs. Added **OpenAI GPT Image 2** as a third selectable provider (input_fidelity=high for identity) alongside Gemini (default) and FLUX; generateAfter now runs a priority chain with auto-fallback, AI_PROVIDER forces one for a bake-off. .env.example documents all three + Nano Banana Pro / gpt-image-2 upgrades. Folder renamed star-fighter -> contour. Pushed to github.com/Stryder-AI/Aesthetics.

- 2026-07-03 · SMOOTH 3D MORPHS: fixed the visible-triangle faceting on the 3D canvas. Root cause: the morph engine is a smooth displacement FIELD, but it was sampled at only 478 landmark vertices, so each big triangle bulk-moved. Fix: uniform 1->4 mesh subdivision x2 levels (src/lib/face/subdivide.ts; ~478 -> ~7.2k verts, ~900 -> ~14.4k tris; originals keep indices so anatomical anchors stay valid) and the field is now evaluated at every vertex. Algorithm unit-checked in Node (midpoint dedup, index integrity, 16x density); in-browser: geometry constructs clean, 2 simultaneous morph events = ~18ms incl. React. 2D warp preview intentionally untouched (owner: not where the problem was). Sculpt brush + highlight + feature scale all operate on the dense mesh now.
- 2026-07-03 · ADMIN AI CONTROL: Settings gains an "AI generation" section (admin-only page): choose None (simulation only) / Auto / Gemini / OpenAI GPT Image / FLUX from the GUI. New GET /api/generate returns provider status (configured flags + resolved model names, never keys); POST accepts a provider override; store persists aiProvider (default auto); VisualizeStep hides Generate + shows an explainer banner when None, tags the button with a forced provider, and includes provider in the generation cache key. Keys remain server-side env only. Verified in-browser: status endpoint, selection persistence, None banner, warp unaffected.
- 2026-07-03 · DECISION (owner): post-IPAAC v2 of the 3D canvas = FLAME / 3DMM dense head model as the FINAL version. A ~5s fit/load time is acceptable. Current subdivision approach is the interim booth solution.
- 2026-07-03 · CANVAS POLISH (owner feedback): (a) Feature highlight was a heavy green additive flood that hid the face. Reworked to a rim-biased highlight (soft mint halo at the region edge + faint interior wash; intensity = 0.85*4w(1-w) + 0.1*w) with layer opacity 0.9->0.5, so the selected feature reads without obscuring the face. (b) "Empty/creepy eyes": the MediaPipe tessellation leaves the eye sockets as holes, which render see-through on the textured mesh. Added buildEyeFill() (geometry.ts) — a triangle fan from each eyelid ring's centroid (ring extracted from FACE_LANDMARKS_LEFT/RIGHT_EYE, angle-ordered; apex UV = ring-average so it samples the eyeball from the photo). Wired into FaceScene before subdivision, so eye-region vertices densify and carry ~0 nose weight (hold still during nose morphs). Fan logic unit-tested in Node (dedup, angular loop closure, index integrity, degenerate guard); build clean; canvas mounts + feature-select stable, no console errors. NOTE: WebGL still can't be screenshotted in the hidden-tab preview, so the final visual (eyes look right, highlight subtle enough) is the owner's to confirm on his screen / live deploy.

- 2026-07-05 · NON-INVASIVE VERTICALS (owner: "same depth as the nose, a MUST"): Lip Filler, Chin Filler and Botox are now fully wired end to end, grounded in a web-research pass on real clinical assessment frameworks. LIP FILLER: 8 sliders (upper/lower vermilion volume, 1:1.6 upper:lower balance, cupid's bow definition, vermilion border, mouth corners / downturn correction, projection-pout, philtral columns) with a full LipFrame morph field (oral-line anchored vertical vermilion expansion + eversion, bow peak lift with preserved central dip, corner blobs, border crisping) driving both the 3D canvas and the 2D warp. CHIN FILLER: 6 sliders (projection to E-line, vertical length, width/taper, labiomental crease, prejowl blend, skin texture) on a ChinFrame scaled by the lip-to-menton unit. BOTOX: 6 sliders (forehead, glabella 11s, crow's feet = AI texture passes; brow lift, masseter slimming, lip flip = live geometric fields from eyebrow landmark sets + gonial oval points). Each template ships its own identity-preserving prompt (banded phrases verified in-browser), injectable-appropriate plan checklist (screening, session, day-14 review, touch-up, maintenance, aftercare), and canvas_handles so the 3D rail is template-driven. New "brow" feature region (highlight/scale). Canvas->AI handoff generalized to any procedure's schema. Texture-only selections show an honest "skin-level, generate to render" hint. Verified in-browser: brief arms each procedure, warp produces after-images for lips/chin/botox geometric fields (~245KB), 3D mesh morphs ~18ms/2 events, botox plan template populates, rhinoplasty regression clean, zero console errors. Production build green.

- 2026-07-05 · IPAAC EVENT UPGRADE (CONTOUR_MASTER_PROMPT work order, T1-T6 in order):
  T1 BOOTH HANDOFF: Vercel Blob inbox (single env var BLOB_READ_WRITE_TOKEN; dev file-store fallback in .booth-dev-store/). POST /api/booth/push (auto after registration + profile "Send to booth screen", idempotent booth ids, offline queue with 12s retry + online listener, visible pending/sent/error state), GET /api/booth/pull (4s poll behind TopNav "Booth link" toggle; merges via registerPatient + consents + photos into IndexedDB using the standard Asset pattern; NEW badge + arrival toast; self-merge guard), DELETE /api/booth/:id + profile "Delete everywhere" cascade (local records, IndexedDB images, server copies; verified 0 orphans), 24h lazy expiry. VERIFIED: push with photo -> pull-merge -> landmark + warp on the pushed photo -> delete everywhere.
  T2 MULTI-PROCEDURE: activeTemplatesFor(interests, primary); collapsible slider section per procedure (primary expanded, per-section + global reset); assembleMultiPrompt (1 active = byte-identical to assemblePrompt — regression verified; multi = one opening + labeled area-scoped blocks + ONE merged preservation footer with no-airbrush caveat); canvas rail = union handles grouped by template; Visualization.procedure joined ("botox+lip_filler+chin_filler"), provider+joined-id cache keys, stale-params hydration fixed (procedure-set match); plans offer every active template + append-items (one-shot guard); report covers all procedures + cross-template slider labels.
  T3 BOTOX ZONE MAP: tappable face schematic (BotoxZoneMap; 6 zones incl. bilateral pairs) replaces flat sliders in Visualize + compact in canvas rail; active zone reveals its intensity slider; texture zones project a mint wash on the before image (object-cover-corrected landmark ellipses) labeled "renders in AI pass", geometric zones keep the live warp; editable units table (botoxUnits.ts: forehead 10-20u, glabella 15-25u, crow's 6-12u/side, brow 4-8u, masseter 20-30u/side, lip flip 4-6u), per-zone stepper prefilled from intensity, running total + cost from Settings "Toxin price per unit (PKR)" (default 1500); toxin_plan persisted on the consultation and rendered in Plan + report. VERIFIED: unit math (86u/PKR129,000), plan card, report block.
  T4 MOBILE: sticky mini before/after on <lg once the stage scrolls out (passive scroll check, NOT IntersectionObserver — IO/rAF/scroll events never fire in hidden tabs); pointer-coarse CSS: 40px chips/tabs/btn-sm, 40px slider hit area w/ background-clip keeping the 6px visual track, larger toggle; two-finger orbit/zoom during Draw/Sculpt (pointer-count tracking enables OrbitControls + cancels live stroke); CameraCapture rear-camera default with flip button + facing-aware mirroring; no overflow at 390px.
  T5 CONSENT PACK: /report/[id]?pack=consent renders ONE premium A4 sheet (verified exactly 296mm): patient/date/procedure chips, before + saved AI after captioned "ILLUSTRATIVE SIMULATION: not a guaranteed outcome" (graceful when no viz), acknowledgement paragraph + per-category risks checklist in ONE editable const (consentPack.ts), actual granted Consent records w/ timestamps + wording version, patient + practitioner signature/date lines. "Print consent pack" on the Report step.
  T6 TIMELINE: Anti-Aging Simulator tab (Brief/Canvas/Visualize/Timeline/Plan/Report). agingPrompts.ts = the ONE editable prompt file (natural vs maintained x +5/+10, shared identity guard, "quietly sobering" tone note). Reuses the generation stack + provider setting; every variant burns AI_DISCLAIMER and persists to IndexedDB under deterministic keys (offline booth insurance); "Pre-generate booth cache (n/4)" button; honest no-key state verified. Full visual accept needs a live provider key: add key -> open each seeded patient's Timeline -> tap pre-generate once.
  SKIPPED: optional sculpt-brush garnish (symmetry/smooth/persist/ring cursor) — deliberately, per the work order's own "reliability beats polish" rule 5 days out; the brush remains a talking tool.

- 2026-07-05 · LIVE-TEST FIXES (owner field report):
  (1) BOOTH GRACEFUL DEGRADATION: a deployment without BLOB_READ_WRITE_TOKEN no longer error-spams. Auto-push after registration silently stands down (boothAvailable=false remembered), the agent never retries not_configured, pull auto-switches the Booth link toggle off, and only a MANUAL "Send to booth screen" shows a calm one-line setup note (info tone, not warning). Verified in-browser with a patched 501 fetch.
  (2) MULTI-VIEW 3D DEPTH — the reason for the 3-photo capture, now real: new src/lib/face/multiview.ts recovers true per-landmark depth from the two turned photos via a closed-form parallax method (per-view in-plane scale fit, right-left u-difference isolates the depth signal with no noisy-z regressor, per-view cos-yaw regression fixes the absolute scale + signs; single-side fallback uses the 35-degree protocol prior with a u-vs-v structure degeneracy detector). Quality gates (yaw 8-65 deg, residual, scale agreement) fall back to the single-view mesh so the canvas can never break. SYNTHETIC VALIDATION (scratch test, ground-truth 3D): depth correlation 0.978 vs 0.609 for MediaPipe-only, rms 2.72px vs 9.46px, yaw recovered within 1 deg, near-frontal inputs rejected. Wired through useFaceData(front, {left, right}) with per-photo + composite IndexedDB caching (misses cached too); CanvasStep shows a "3-view depth" / "Front-photo depth" provenance chip (verified in-browser incl. the degenerate-sides fallback; an earlier alternating pose/structure approach DIVERGED on noisy mp-z seeds and was replaced). 2D warp/UVs untouched (x,y stay front-photo).
  (3) CAPTURE GUIDES: poses renamed "Turned right/left" with explicit "rotate, like checking your shoulder, keep the chin level, do not tilt" copy; ghost overlay no longer rotates the oval (which read as a tilt) — it now shows a narrowed oval, a vertical facial midline swung toward the nose direction, a nose wedge and a turn arrow, mirror-aware.
  (4) CANVAS RAIL UX: combined-procedure handles are an exclusive-open accordion (primary open, click to switch, active-count badges) and the 3D stage (plus the Visualize compare stage) is position:sticky on desktop, so the model never scrolls out of view (verified: sticky top 84px at 1280px).

- 2026-07-05 · BOOTH LIVE ON PRODUCTION: walked the owner through connecting the Blob store, then adapted to what Vercel actually provisions today: (a) toggle now probes before enabling + guided setup dialog (no more on/off flicker); (b) token detection accepts custom-prefixed *_READ_WRITE_TOKEN values AND the new token-less connect flow (BLOB_STORE_ID + platform credentials; 501 diagnostics list blob-like env key names); (c) the store was created PRIVATE (current Vercel default, better for medical photos) so writes adapt private-first/public-fallback, inbox JSONs are read via the SDK server-side, and photos are NEVER public URLs: pull responses point at /api/booth/photo, a path-validated same-origin route streaming bytes with store credentials. VERIFIED LIVE against aesthetics-mu.vercel.app: push -> pull (item + photo route URL) -> photo streams HTTP 200 -> delete -> inbox empty.

- 2026-07-06 · 3D INVERSION FIX + TIMELINE YEARS + HAIR TRANSPLANT (owner field report from a real booth-uploaded patient):
  (1) INSIDE-OUT 3D MESH FIXED AT THE ROOT: the two-view parallax solver had an unresolved mirror ambiguity (the classic two-view Necker/bas-relief case: depth negated + both yaws negated satisfies the equations with IDENTICAL residuals, so the candidate loop's tie-break picked a sign by photo-order convention, not anatomy). REPRODUCED synthetically: swapping the side-photo order flipped the refined depth to corr -0.955 with every quality gate still passing — exactly the hollow-shell face the owner photographed. Fix in src/lib/face/multiview.ts: the global sign is now resolved against the MediaPipe relief direction (weak on magnitude, reliable on nose-toward-camera; correlation < 0.15 = cannot orient = fall back), and a final anatomical guard refuses ANY fit whose nose tip is not clearly toward the camera (both branches), so an inverted mesh can never ship — worst case is always the old single-view relief. The synthetic test now proves both photo orders give corr +0.978 with consistent per-side yaws, and an adversarially inverted prior is refused. Composite depth cache version-bumped (mv_ -> mv2_ in hooks.ts) so already-cached inverted fits auto-recompute on next open. OrbitControls azimuth widened 0.42pi -> 0.46pi (~83 deg) for a near-true profile view; full 360 stays intentionally clamped because a front-photo relief has no back of head to show.
  (2) TIMELINE +20/+30 YEARS: AgingYears extended to 5|10|20|30 with honest natural-course and maintained-course prompt copy for both new horizons (20y: deep static lines, marked volume loss, jowling, substantial graying; 30y: senior years, marked laxity, largely gray hair — maintained branches age gracefully, never frozen); year chips now Now/+5/+10/+20/+30; booth pre-generate cache counts n/8.
  (3) HAIR TRANSPLANT VERTICAL + HAIRSTYLE TRY-ON: fifth live template (category "hair", new Brief row "Hair Restoration"). Restoration goal sliders (hairline restore, temple points, overall density, crown coverage) + an exclusive hairstyle try-on chip row (Keep current + 8 styles: classic taper, textured crop, side part, slicked back, pompadour, natural curls, buzz, long flow) stored as flat style_*=100 params with new bandless SliderDef phrases (no "noticeably" adverb on on/off choices), so persistence, params hash, multi-procedure prompts and report labels all work unchanged. 12-months-grown-in prompt with a hard identity guard (face untouched, natural irregular hairline, own hair color unless a style says otherwise); AREA_SCOPE entry for combined consults; full FUE plan template (trichoscopy staging -> procedure day -> day-3 wash -> shedding counselling -> 4/8/12-month reviews; graft care + finasteride/minoxidil maintenance); hair-specific consent-pack risks block (shock loss, graft survival, donor dot scars, folliculitis, ongoing native loss). VERIFIED IN-BROWSER: Brief shows the Hair Restoration category; Visualize renders sliders + try-on chips, style picks are exclusive and the assembled instruction reads clean ("Restyle the grown-in hair as a soft pompadour..."); Plan applies the FUE pathway; Timeline shows all five year chips with the 0/8 counter; no console errors; production build passes.

- 2026-07-06 · HIGGSFIELD PROVIDER (owner could not get a Gemini key; supplied a Higgsfield Cloud key id+secret): fourth provider wired end to end in src/lib/server/providers.ts against platform.higgsfield.ai (auth "Authorization: Key id:secret"; presigned upload POST /files/generate-upload-url -> PUT bytes -> public_url; submit POST /{model}; poll GET /requests/{id}/status through queued|in_progress -> completed|failed|nsfw|canceled; result image downloaded and returned base64). Default model higgsfield-ai/soul/reference (the router exposes soul modes standard|character|reference; nano-banana/seedream/gpt-image paths 404 on this account). The reference-image field name is not publicly documented, so submit self-discovers it from a small candidate ladder (image_url -> image -> reference_image_url), pins the first accepted name, and surfaces FastAPI 422 validation detail verbatim on hard mismatch. Provider selectable in admin Settings ("Higgsfield (Soul)") and via AI_PROVIDER=higgsfield; env HIGGSFIELD_API_KEY + HIGGSFIELD_API_SECRET (+ optional HIGGSFIELD_IMAGE_MODEL). VERIFIED with the live key: credentials accepted, GET /api/generate reports higgsfield configured+active, Settings shows "Key configured", and a real POST walks the full path to Higgsfield's credit wall — the account's API wallet is EMPTY (403 not_enough_credits; the API bills separately from the owner's 771-credit MAX app subscription), mapped to an actionable error naming cloud.higgsfield.ai Billing. Awaiting owner top-up for the funded end-to-end + identity-fidelity check; quality expectation set honestly (soul/reference is reference-guided generation, not surgical pixel-faithful editing a la Nano Banana).

- 2026-07-06 · MODEL PICKER + DOWNLOADS + REVE DEFAULT (owner field report: production said "no AI keys configured" — root cause: API keys are secrets and do NOT travel through git pushes; they must be set as Vercel env vars, which the owner's dashboard session can do): (a) probed the Higgsfield router exhaustively for Nano Banana (18 path spellings) — NOT exposed on this account's API; discovered **reve/edit** (instruction-driven photo EDITING, schema {prompt, image_url}, validated to the credit wall) and made it the Higgsfield default over soul/reference. (b) Admin Settings now has a per-provider MODEL PICKER (friendly-name chips: Nano Banana/Pro, GPT Image 1/2, Kontext Pro/Max, Reve Edit, Soul Reference/Character); choices persist in the store (aiModels), ride every generate request, and are WHITELISTED server-side (MODEL_OPTIONS) so arbitrary client strings can never reach a provider URL; picking the server default clears the override; provider+models are folded into the generation cache keys (Visualize + Timeline). (c) Stage downloads: with any after visible, "After" downloads the disclaimered after photo and "Before + after" composes a labeled side-by-side JPG (BEFORE/AFTER pills, disclaimer strip) — booth takeaway. Verified in-browser: picker chips render/select/persist/revert, GET /api/generate returns options + reve/edit default, downloads run clean.

- 2026-07-06 · CALENDAR + VYBERO ANALYTICS (owner: "we currently only have a dashboard and patients, which is not enough"; modeled on H:\Systems\VYBERO\VYBERO_Analytics_0.1, the TechGIS ElevenLabs-agent analytics app): (1) CALENDAR module (/calendar, all roles): week grid desktop / single-day mobile from configurable clinic hours (Settings section: open/close/slot length/open days), tap-a-free-slot booking modal (patient datalist linking, type, duration, procedure interest, notes), appointment detail modal with status flow (booked -> confirmed -> completed / no-show / cancelled), source badges (front desk / VYBERO phone / booth). (2) VYBERO INTEGRATION API (Blob-backed shared appointment book + call log, boothStore pattern deliberately duplicated not refactored): GET /api/vybero/availability?date= (free slots from hours minus active bookings), POST /api/vybero/book (agent key or same-origin; 409 slot_taken guard; upsert by id; agent bookings auto-tagged source=vybero), GET /api/vybero/appointments, POST+GET /api/vybero/call-log; agent auth = x-vybero-key header vs VYBERO_API_KEY env (dev mode open); VyberoAgent 20s pull merges server book into local store (501 stands down for session). Clinic-side creates/status changes push up so agent availability stays truthful. (3) VYBERO ANALYTICS module (/analytics, admin-gated): KPI row (calls 14d, this week, booked via calls, conversion %, avg duration, missed), stacked call-volume chart (booked/handled/missed, hand-rolled SVG — no chart dependency), call outcomes bars, "What patients ask about" procedure/topic mining (keyword taxonomy adapted from the TechGIS deep-analytics products/industries approach; agent-provided topics[] win, summary keyword-detect backfills), grouped Top Questions with counts, busiest calling hours, recent calls table with topic chips + outcome badges. (4) SEEDED DEMO STORY: deterministic (mulberry32) two-week narrative — ~50 calls (weighted topics/questions/outcomes, Urdu/English/Punjabi) + ~40 appointments incl. VYBERO-sourced ones linked to their calls; seedIfNeeded BACKFILLS devices that seeded before this module existed; resetDemo regenerates. VERIFIED IN-BROWSER: calendar renders 34 seeded blocks + live free-slot count; FULL AGENT ROUND-TRIP: availability (18 slots) -> book (Khadija Rehman, auto source=vybero) -> double-book correctly 409 slot_taken -> VyberoAgent pull -> appointment appears on the calendar; analytics shows 52 calls / 45% conversion / grouped questions / charts; no console errors; build passes (18 routes).

## Current state

Production build passes. Five live procedure verticals (Rhinoplasty, Lip Filler, Chin Filler, Botox, Hair Transplant with hairstyle try-on) with combined multi-procedure consultations, Botox zone-map + units/cost planning, phone-to-desktop booth handoff (Vercel Blob, LIVE on production), mobile-comfort pass, one-page consent pack, the Anti-Aging Timeline tab (Now/+5/+10/+20/+30, natural vs maintained), a bookable CALENDAR wired to the VYBERO phone agent (availability + booking API, shared Blob appointment book, 20s clinic sync), and the admin VYBERO ANALYTICS module (call KPIs, topic/question mining, volume + peak-hour charts, recent calls). Multi-view depth is orientation-guaranteed (anatomical sign resolution + nose-out guard; an inverted mesh cannot ship). AI providers: Gemini/OpenAI/FLUX/Higgsfield with an admin model picker; Higgsfield live on prod awaiting API-wallet credits. Pre-event checklist: top up Higgsfield API credits (or add GEMINI_API_KEY), set VYBERO_API_KEY for the real agent, run the model bake-off, pre-generate the Timeline booth cache per seeded patient (8 variants). Post-event milestone: FLAME/3DMM canvas.
